//using + as a string operator to add strings 
const  firstName = "Young"
const clanName = "DeJounte"
const streetName = "Greatness"

let fullName;

fullName = firstName  + "" + clanName + "" + streetName;
     console.log(`${fullName}`);